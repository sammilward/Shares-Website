import { SpecificCurrencyPipe } from './specific-currency.pipe';

describe('SpecificCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecificCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
